{
  "name": "com.xacce.blobcheg",
  "version": "0.1.0",
  "displayName": "Blobcheg",
  "description": "A separated blob database over binary files: its own buffer, reads by reinterpretation at an offset, no BlobAssetReference.",
  "unity": "6000.3",
  "dependencies": {
    "com.unity.burst": "1.8.29",
    "com.unity.collections": "2.6.8",
    "com.unity.mathematics": "1.3.2"
  },
  "samples": [
    {
      "displayName": "Destructive tests (core dev)",
      "description": "An end-to-end set that breaks the package from the asset down to a byte in the file. It does not enter the compilation until you import it: this is a tool for the package developer and not for the consumer.",
      "path": "Samples~/AdvancedTests"
    }
  ]
}
